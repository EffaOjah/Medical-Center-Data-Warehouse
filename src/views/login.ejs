<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>

    <style>
        body {
            display: flex;
            justify-content: center;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        }
        h1 {
            background-color: rgb(228, 86, 121);
            color: white;
            padding: 20px;
        }
        .container form p {
            margin: 10px;
            font-size: 20px;
        }
        .container form input{
            padding: 10px;
            margin: 10px;
            width: 500px;
            font-size: 20px;
            border: none;
            border-radius: 5px;
            border: solid 2px rgba(0, 0, 0, 0.2);
        }
        .container form button {
            padding: 10px;
            margin: 10px;
            background-color: rgb(228, 86, 121);
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Login as, <%= locals.role %></h1>
        <br>

        <form action="/<%= locals.role %>-login" method="post">
            <input type="hidden" name="role" value="<%= locals.role %>">
            <p>Email:</p>
            <input type="email" id="email" name="email" required><br><br>

            <p>Password:</p>
            <input type="password" id="password" name="password" required><br><br>

            <button type="submit">Submit</button>
        </form>
    </div>
</body>
</html>